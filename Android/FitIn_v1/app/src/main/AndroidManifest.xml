<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitin_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitIn_v1">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashScreenStyle"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="메인화면"/>
        <activity android:name=".LoginActivity" android:label="로그인"/>
        <activity android:name=".RegisterActivity" android:label="회원가입"/>
        <activity android:name=".Register_2Activity" android:label="회원가입2"/>
        <activity android:name=".WebviewActivity" android:label="웹뷰"/>
        <activity android:name=".CompleteActivity" android:label="완료"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>